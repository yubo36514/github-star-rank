# 统一换行符：仓库内统一存 LF，避免 Windows 开发环境把 CRLF 带入 Docker/Linux 构建
* text=auto eol=lf

# 二进制 / 不应被改写的产物
*.png binary
*.jpg binary
*.ico binary
*.db binary
*.woff binary
*.woff2 binary
uv.lock -diff linguist-generated
